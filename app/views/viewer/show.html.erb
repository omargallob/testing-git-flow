<div class="grids">
<h1><%= @page.title %></h1>
<p><%= @page.body %></p>

<% if @page.name =="home" %>

		<div id="posts" class="holder">
			<%= render :partial => @posts %>
			<%= render :partial => @posts %>
			<%= render :partial => @posts %>
		</div>

	<% content_for(:script) do %>
		<script>
		$(function(){


				$('#posts').masonry({
		    // options
			    itemSelector : '.post',
		   		columnWidth : 235
			  });	

		  
		});
		</script>
	<% end %>
<% end %>
</div>