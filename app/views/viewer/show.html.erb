<div class="grids">
<h1><%= @page.title %></h1>
<div class="fb-like" data-href="http://ragboneman.com/" data-send="false" data-width="450" data-show-faces="false"></div>
<p><%= @page.body %></p>

<% if @page.name =="home" %>

		<div id="posts" class="holder">
			<%= render :partial => @posts %>
			<%= render :partial => @posts %>
			<%= render :partial => @posts %>
		</div>

	<% content_for(:script) do %>
		<script>
		$(function(){


				$('#posts').masonry({
		    // options
			    itemSelector : '.post',
		   		columnWidth : 235
			  });	

		  
		});
		</script>
	<% end %>
<% end %>
</div>