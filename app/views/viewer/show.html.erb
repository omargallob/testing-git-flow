<div id="postscontainer" class="grids main">
<div id="top-header">
<h1><%= @page.title %></h1>

<div class="fb-like" data-href="<%= request.url %>" data-send="false" data-show-faces="false" data-colorscheme="light" ></div> 
</div>
<% unless @page.name =="home" %>

<%= content_tag :div, @page.body.html_safe, :id=>"main-content" unless  @page.body.blank? %>

<% end %>
<% if  @page.name =="home" %>
		<div id="posts" class="holder">
			<%= render :partial => @posts %>
		</div>
<% end %>

	<% content_for(:script) do %>
		<script>
		$(function(){

			var $container = $('#posts');
			$container.imagesLoaded( function(){
				$container.masonry({
				// options
				  itemSelector : '.post',
						columnWidth : 235
				});	
			});		  
		});
		</script>
	<% end %>

</div>

